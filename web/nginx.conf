server {
    listen 80;
    root /usr/share/nginx/html;

    # SPA em /app: rota desconhecida cai no index (React Router resolve)
    location /app/ {
        try_files $uri /app/index.html;
    }
    location = / {
        return 302 /app/;
    }
}
